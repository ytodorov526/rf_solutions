{
  "name": "rf-solutions-server",
  "version": "1.0.0",
  "description": "Backend server for RF Solutions engineering company",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required for server\"",
    "azure-deploy": "npm ci && node src/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "rf",
    "antenna",
    "radar",
    "engineering",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
